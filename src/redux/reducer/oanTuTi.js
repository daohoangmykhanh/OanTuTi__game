const stateDefault = {
    soBanChoi:0,
    soBanThang:0,
    banChon:true,
}

export const oanTuTiReducer = (state = stateDefault, action) => {
    switch(action.type){
        default: return state;
    }
}